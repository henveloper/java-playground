package leetcode;

/**
 * trivial quadratic time soln
 */
class Solution {
    public String longestPalindrome(String s) {
        int maxPalindromeLen = 1;
        int n = s.length();

        if (n % 2 != 0) {
            int mid = n/2;

        }
        for (int midIndex = s.length();)
    }
}